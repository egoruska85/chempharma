<nav class="navbar navbar-expand-lg bg-body-tertiary" style="background-color: #e3f2fd;">
  <div class="container-fluid">
    <%= link_to root_path, class:"nav-link", style:"font-family: bankgothic; font-weight: 900; font-size: 20px;" do %>
      CHEMPHARMA
    <% end %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Продукция", products_path, class:'nav-link active' %>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Link</a>
        </li>

        <li class="nav-item">
          <a class="nav-link disabled">Disabled</a>
        </li>
      </ul>
      <% if current_user == nil %>
        <%= link_to new_user_session_path, class:'btn btn-success' do %>
          Войти
        <% end %>
        <% else %>
          <%= link_to edit_user_registration_path(current_user), class:'btn btn-primary' do %>
            <%= current_user.username.capitalize %>
          <% end %>
      <% end %>

|

      <% if current_user != nil %>
        <%= button_to destroy_user_session_path, method: :delete, class:'btn btn-danger' do %>
          Выйти
        <% end %>
      <% end %>
    </div>
  </div>
</nav>
